<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.futao.mybatis.cache.dao.TempDap">
    <select id="byId" resultType="com.futao.mybatis.cache.entity.TempEntity">
        SELECT *
        FROM test.temp
        WHERE id=#{id}
    </select>
</mapper>